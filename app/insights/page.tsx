import type { Metadata } from 'next'; import { SiteFooter, SiteNav } from '../../components/SiteNav'; import { articles } from '../../lib/articles';
export const metadata: Metadata={title:'Custom Packaging Guides | MTT Packaging',description:'Professional guides to custom box structures, materials, printing finishes and product inserts.',alternates:{canonical:'/insights'}};
export default function InsightsPage(){return <main><SiteNav/><header className="page-hero"><p>Packaging knowledge</p><h1>Better inputs lead to a better box.</h1><p>Practical guidance for buyers before sampling, specification and quotation.</p></header><section className="insights standalone-insights"><div className="insight-grid">{articles.map(a=><article key={a.slug}><span>{a.number} / {a.angle}</span><h3>{a.title}</h3><p>{a.summary}</p><a href={`/insights/${a.slug}`}>Read the guide →</a></article>)}</div></section><SiteFooter/></main>}
