import {packagingDetails} from '../lib/packaging-details';
export default function PackagingDetailNotes({kind}:{kind:string}){const notes=packagingDetails[kind];if(!notes)return null;return <section className="packaging-detail-notes"><header><p className="section-kicker">Details to specify</p><h2>The details that make it yours.</h2><p>Use these points to turn a visual direction into a clear packaging brief.</p></header><div>{notes.map(([title,copy],i)=><article key={title}><span>0{i+1}</span><h3>{title}</h3><p>{copy}</p></article>)}</div><a className="visual-case-link" href="/request-a-quote">Discuss these details with MTT →</a></section>}
