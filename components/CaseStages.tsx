import {caseStages} from '../lib/case-stages';
export default function CaseStages({slug}:{slug:string}){const item=caseStages[slug];if(!item)return null;const split=({fragrance:603/1254,electronics:594/1254,modular:585/1254} as Record<string,number>)[item.id] || .5;return <section className="case-visual-story" aria-label="Packaging solution in four stages"><p className="case-concept-label">Packaging solution analysis</p><h2>From product to finished presentation</h2><p>Four stages explain the product requirements, structure and finishing choices. AI-generated visualizations; final construction and finish are confirmed through physical sampling.</p><div className="case-stage-grid">{item.captions.map((caption,i)=><figure key={i}><div className={`case-stage-panel panel-${i}`}><img style={{height:`${100/(i<2?split:1-split)}%`,top:i<2?0:`${-100*split/(1-split)}%`}} src={item.image} alt={`${['Product requirements','Structure direction','Materials and finishing','Complete packaging presentation'][i]}: ${caption}`} width="1600" height="1600" loading="lazy"/></div><figcaption><h3>{String(i+1).padStart(2,'0')} · {['Product requirements','Structure direction','Materials & finishing','Complete packaging presentation'][i]}</h3><p>{caption}</p></figcaption></figure>)}</div></section>}
