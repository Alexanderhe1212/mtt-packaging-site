'use client';
import {useEffect,useState} from 'react';
import {products,categories,structures,wrappingPapers} from '../lib/products';
import {ProductView} from './ProductGallery';
const displayOrder=['MTT-R0101','MTT-R0202','MTT-R0401','MTT-R0507','MTT-R0107','MTT-R0303','MTT-R0209','MTT-R0402','MTT-R0304','MTT-R0501','MTT-R0602','MTT-R0604'];
const catalogueProducts=[...products].sort((a,b)=>(displayOrder.includes(a.code)?displayOrder.indexOf(a.code):999)-(displayOrder.includes(b.code)?displayOrder.indexOf(b.code):999));
export default function ProductCatalogue({zh=false}:{zh?:boolean}){
 const [family,setFamily]=useState('rigid');
 useEffect(()=>{const f=new URLSearchParams(location.search).get('family');if(['rigid','carton','corrugated','bag'].includes(f||''))setFamily(f!)},[]);
 const [query,setQuery]=useState(''),[category,setCategory]=useState(''),[structure,setStructure]=useState(''),[wrap,setWrap]=useState('');
 const found=catalogueProducts.filter(p=>(!family||p.family===family)&&(!category||p.category===category)&&(!structure||p.structure===structure)&&(!wrap||p.wrap===wrap)&&[p.name,p.nameZh,p.code,p.categoryName,p.categoryZh,p.structureName,p.structureZh,p.material,p.materialZh,p.wrap,p.wrapZh,p.finish,p.finishZh].join(' ').toLowerCase().includes(query.toLowerCase().trim()));
 return <>
 <div className="product-categories product-family-tabs" role="group" aria-label={zh?'包装大类':'Packaging families'}>{[['','All packaging','全部包装'],['rigid','Rigid gift boxes','精品硬盒'],['carton','Folding cartons','卡纸盒'],['corrugated','Corrugated boxes','瓦楞盒'],['bag','Paper bags','手提袋']].map(([id,en,cn])=><button key={id} aria-pressed={family===id} onClick={()=>{setFamily(id);setCategory('');setStructure('');setWrap('')}}>{zh?cn:en}</button>)}</div>
 <section className="product-family" aria-label={zh?'包装类别':'Packaging family'}><span>{zh?'当前类别':'Collection'}</span><h2>{zh?'按结构与用途选包装':'Choose by construction and application'}</h2><p>{zh?'比较精品硬盒、卡纸盒、瓦楞盒与纸袋。默认展示精品硬盒；飞机盒属于瓦楞包装，可切换大类查看，材料和开合结构分别展示。':'Compare rigid boxes, folding cartons, corrugated packaging and bags. Rigid gift boxes are shown first. Roll-end mailers belong to corrugated packaging; use the family tabs to compare constructions.'}</p></section>
 <div className="product-categories product-application-tabs" role="group" aria-label={zh?'选择产品用途':'Choose an application'}>{categories.filter(c=>products.some(p=>(!family||p.family===family)&&p.category===c.id)).map(c=><button type="button" key={c.id} aria-pressed={category===c.id} onClick={()=>setCategory(category===c.id?'':c.id)}><b>{zh?c.zh:c.en}</b><span>{products.filter(p=>(!family||p.family===family)&&p.category===c.id).length} {zh?'款':products.filter(p=>(!family||p.family===family)&&p.category===c.id).length===1?'design':'designs'}</span></button>)}</div>
 <div className="product-filters"><label>{zh?'搜索产品、材料或工艺':'Search products, materials or finishes'}<input type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder={zh?'例如：抽屉、烫金、香水':'Try drawer, foil, perfume'}/></label><label>{zh?'产品用途':'Application'}<select value={category} onChange={e=>setCategory(e.target.value)}><option value="">{zh?'全部用途':'All applications'}</option>{categories.filter(c=>products.some(p=>(!family||p.family===family)&&p.category===c.id)).map(c=><option key={c.id} value={c.id}>{zh?c.zh:c.en}</option>)}</select></label><label>{zh?'开合结构':'Opening structure'}<select value={structure} onChange={e=>setStructure(e.target.value)}><option value="">{zh?'全部结构':'All structures'}</option>{structures.filter(s=>products.some(p=>(!family||p.family===family)&&p.structure===s.id)).map(s=><option key={s.id} value={s.id}>{zh?s.zh:s.en}</option>)}</select></label><label>{zh?'外裱纸':'Wrapping paper'}<select value={wrap} onChange={e=>setWrap(e.target.value)}><option value="">{zh?'全部裱纸':'All papers'}</option>{wrappingPapers.filter(w=>products.some(p=>(!family||p.family===family)&&p.wrap===w.id)).map(w=><option key={w.id} value={w.id}>{zh?w.zh:w.en}</option>)}</select></label></div>
 <div className="product-results"><p role="status">{found.length} {zh?'款包装':found.length===1?'packaging design':'packaging designs'}</p>{(family||query||category||structure||wrap)&&<button type="button" onClick={()=>{setFamily('rigid');setQuery('');setCategory('');setStructure('');setWrap('')}}>{zh?'清除筛选':'Clear filters'}</button>}</div>
 <div className="product-grid">{found.map(p=><a className="product-card" key={p.code} href={`${zh?'/zh':''}/products/${p.slug}`}><ProductView src={p.image} alt={zh?p.nameZh:p.name}/><div><small>{zh?p.familyZh:p.familyName} · {p.code} · {zh?p.structureZh:p.structureName}</small><h2>{zh?p.nameZh:p.name}</h2><p>{zh?p.wrapZh:p.wrap} · {zh?p.finishZh:p.finish}</p><b>{zh?'查看五个视角及规格 →':'Explore views & specifications →'}</b></div></a>)}</div>
 {!found.length&&<p>{zh?'没有匹配的产品，请调整筛选条件。':'No matching designs. Try another filter.'}</p>}
 </>;
}
