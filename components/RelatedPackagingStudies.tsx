import {visualStudies} from '../lib/visual-cases';
export default function RelatedPackagingStudies({ids}:{ids:string[]}){return <section className="related-packaging-studies"><header><p className="section-kicker">Packaging in detail</p><h2>See how the choices come together.</h2><p>Explore the product, structure and finishing decisions behind a related packaging study.</p></header><div>{ids.map(id=>{const [slug,title]=visualStudies[id];return <a href={`/insights/${slug}`} key={id}><img src={`/design/case-stages/${id}.webp`} alt={`Four stages: ${title}`} width="1254" height="1254" loading="lazy"/><h3>{title}</h3><span>Explore the study →</span></a>})}</div></section>}
